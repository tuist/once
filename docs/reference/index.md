# Reference

Looked-up material that mirrors the code: synopsis, flags, exit
codes, and other surface that callers reach for at the keyboard.

Start with the [CLI Reference](/reference/cli/) for every subcommand
the `once` binary exposes, with its synopsis, options, and arguments.
