# Build context for web/Dockerfile is the repo root. Keep it small: the image
# only needs `crates/` (for the `once` binary) and `web/` (the Phoenix app).
.git
target
docs

web/_build
web/deps
web/node_modules
web/assets/node_modules
web/priv/static/assets
web/priv/static/docs/search-index.json
web/erl_crash.dump
web/tmp

**/.DS_Store
