set -eu
[ "$ACTION" = build ]
[ "$TARGET_NAME" = 'Script App' ]
[ "$CONFIGURATION" = Debug ]
[ "$SRCROOT" = "$PROJECT_DIR" ]
[ "$SOURCE_ROOT" = "$SRCROOT" ]
[ "$PROJECT_NAME" = Scripts ]
[ "$PLATFORM_NAME" = macosx ]
[ -d "$SDKROOT" ]
[ "$ARCHS" = "$CURRENT_ARCH" ]
[ "$TARGET_BUILD_DIR" = "$CONFIGURATION_BUILD_DIR" ]
[ "$TARGET_BUILD_DIR" = "$BUILT_PRODUCTS_DIR" ]
[ "$CUSTOM_OUTPUT_DIR" = "$TARGET_TEMP_DIR/custom" ]
[ "$SCRIPT_INPUT_FILE_COUNT" = 1 ]
[ "$SCRIPT_INPUT_FILE_LIST_COUNT" = 1 ]
[ "$SCRIPT_OUTPUT_FILE_COUNT" = 1 ]
[ "$SCRIPT_OUTPUT_FILE_LIST_COUNT" = 0 ]
[ -f "$SCRIPT_INPUT_FILE_LIST_0" ]
cat "$SRCROOT/input.txt" > "$SCRIPT_OUTPUT_FILE_0"
