# Changelog

All notable changes to this project will be documented in this file.

## Unreleased

### Breaking Changes

- Script actions now live under `[[target]]` with `rule = "script"` and
  `[target.script]`. Existing `[[task]]` entries must be migrated to the new
  form before upgrading.
- Inline script rules now use the same public contract names as annotated
  script files: `input`, `output`, `env`, and `cwd`.

<!-- generated by git-cliff -->
